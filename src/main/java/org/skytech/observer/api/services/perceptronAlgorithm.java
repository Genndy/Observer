package org.skytech.observer.api.services;
import org.skytech.observer.perceptron.models.Perceptron;

/*
Perceptron teaching algorithm have 3 step.
 */
public class perceptronAlgorithm {
    Perceptron perc;
    public perceptronAlgorithm(Perceptron perc){
        this.perc = perc;
    }
    // step 1 input getting into perceptron
    void inputInit(){
        // so... where from we have to get inputs?
        // first - we have to find data source
    }
    // step 2 reverse-error invasion
    // step 3 weight corrective
}
